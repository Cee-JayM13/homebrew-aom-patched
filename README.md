# Cee-jaym13 Aom-patched

## How do I install these formulae?

`brew tap cee-jaym13/aom-patched`
then, after performing the tap command,
`brew install --HEAD --build-from-source cee-jaym13/aom-patched/aom-v2`

Or `brew tap cee-jaym13/aom-patched` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "cee-jaym13/aom-patched"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
